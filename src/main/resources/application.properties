spring.datasource.username=${JDBC_DATABASE_USERNAME:tsiupryk}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:123}
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://localhost:5432/home}
spring.datasource.driver-class-name=org.postgresql.Driver

server.port=8080
